# Clean input while solving
# because array is rigid 
P₁ ← (
  ↧⊃(≤3/↥|≠0/↧)⌵≡/-◫2.
  /+×↥(⊃(≍⇌⍆|≍⍆)).:
)
P₂ ← ↥⊃P₁/↥≡P₁≡▽⊙¤⊞≠.°⊏

∩/+⊜(⊃P₂P₁⊜⋕≠@ .)¬⦷"\r\n". &fras"input"

# This would work if the array wasn't rigid T-T
# F  ← ⊜(⊜⋕≠@ .)¬⦷"\r\n".
# P₁ ← (
#   ↧⊓(≤3|≠0)⊃(/↥|/↧)⍉⌵≡(≡/-◫2).
#   /+=↥≡(⊃(≍⇌⍆|≍⍆)).:)
